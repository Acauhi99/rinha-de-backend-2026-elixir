## Agent skills

### Issue tracker

Issues deste repo vivem no GitHub Issues (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Mapeamento usa labels padrão (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Layout single-context: usar `CONTEXT.md` na raiz + `docs/adr/`. See `docs/agents/domain.md`.
